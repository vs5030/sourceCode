import { SecurityFormControl } from './security-form-control';

describe('SecurityFormControl', () => {
  it('should create an instance', () => {
    expect(new SecurityFormControl()).toBeTruthy();
  });
});
