import { CstmNgForDirective } from './cstm-ng-for.directive';

describe('CstmNgForDirective', () => {
  it('should create an instance', () => {
    const directive = new CstmNgForDirective();
    expect(directive).toBeTruthy();
  });
});
