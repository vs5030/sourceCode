import { CstmNgClassDirective } from './cstm-ng-class.directive';

describe('CstmNgClassDirective', () => {
  it('should create an instance', () => {
    const directive = new CstmNgClassDirective();
    expect(directive).toBeTruthy();
  });
});
