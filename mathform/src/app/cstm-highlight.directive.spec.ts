import { CstmHighlightDirective } from './cstm-highlight.directive';

describe('CstmHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new CstmHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
